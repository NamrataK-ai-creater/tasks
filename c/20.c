 //Write a program to find the square root of a given number (use sqrt () function)?Math.sqrt(a)
#include <stdio.h>
int main() {
    float number;
    printf("Enter a number: ");
    scanf("%f",&number);
    float square root =sqrt(number);
    printf("The square root of is: %f\n",square_root);

    return 0;
}
