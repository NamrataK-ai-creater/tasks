// Write a program to find the square and cube of a given number?
#include <stdio.h>
int main()
 {
    float number, square, cube;
    printf("Enter a number: ");
    scanf("%f", &number);
    square = number * number;
    cube = number*number*number;

   
    printf("The square of is: %f\n",square);
    printf("The cube of is:%f\n",Acube);

    return 0;
}
